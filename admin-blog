<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Blog | Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;700;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body { font-family: 'Manrope', sans-serif; background-color: #1a202c; color: #e2e8f0;}
        .logo-font { font-family: 'JetBrains Mono', monospace; }
        .gold-text { color: #D4AF37; }
        .sidebar { background-color: #2d3748; }
        .sidebar-link { transition: all 0.2s ease-in-out; }
        .sidebar-link:hover, .sidebar-link.active { background-color: #D4AF37; color: #1a202c; transform: translateX(5px); }
        .sidebar-link.active i, .sidebar-link:hover i { color: #1a202c; }
        .form-input, .form-textarea { background-color: #4a5568; border: 1px solid #718096; }
        .form-input:focus, .form-textarea:focus { border-color: #D4AF37; box-shadow: 0 0 0 2px rgba(212, 175, 55, 0.2); outline: none; }
        .btn-primary { background-color: #D4AF37; color: #1a202c; font-weight: 700; }
        .btn-primary:hover:not(:disabled) { background-color: #c09b30; }
        .btn-secondary { background-color: #4a5568; }
        .btn-danger { background-color: #c53030; }
    </style>
</head>
<body class="flex">

    <!-- Sidebar Navigation -->
    <aside class="w-64 min-h-screen sidebar p-4 fixed">
        <h1 class="text-2xl font-bold text-center logo-font gold-text mb-10">CodeWithMee</h1>
        <nav class="space-y-2">
            <a href="admin-dashboard.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-tachometer-alt w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Dashboard</span></a>
            <a href="admin-courses.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-book w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Courses</span></a>
            <a href="admin-projects.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-project-diagram w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Projects</span></a>
            <a href="admin-gallery.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-images w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Gallery</span></a>
            <a href="admin-quiz.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-question-circle w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Quiz</span></a>
            <a href="admin-about.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-info-circle w-6 text-center gold-text"></i><span class="ml-4 font-semibold">About Page</span></a>
            <a href="admin-links.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-link w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Social Links</span></a>
            <a href="admin-submissions.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-envelope-open-text w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Submissions</span></a>
            <a href="admin-accounts.html" class="sidebar-link flex items-center p-3 rounded-lg"><i class="fas fa-user-shield w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Admin Accounts</span></a>
            <a href="admin-blog.html" class="sidebar-link active flex items-center p-3 rounded-lg"><i class="fas fa-blog w-6 text-center gold-text"></i><span class="ml-4 font-semibold">Blog</span></a>
        </nav>
        <div class="absolute bottom-4 left-4 right-4">
             <button id="logout-btn" class="w-full flex items-center p-3 rounded-lg bg-red-800 text-red-100 hover:bg-red-700"><i class="fas fa-sign-out-alt w-6 text-center"></i><span class="ml-4 font-semibold">Logout</span></button>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="ml-64 flex-1 p-10">
        <h2 class="text-3xl font-bold mb-8">Manage Blog Posts</h2>
        
        <div class="bg-gray-800 p-6 rounded-lg mb-8">
            <h3 id="form-title" class="text-xl font-bold mb-4 gold-text">Create New Post</h3>
            <form id="blog-form" class="space-y-4">
                <input type="hidden" id="post-id">
                <div>
                    <label for="post-title" class="block text-sm font-medium text-gray-400">Post Title</label>
                    <input type="text" id="post-title" class="w-full mt-1 p-2 rounded-md form-input" required>
                </div>
                 <div>
                    <label for="post-image" class="block text-sm font-medium text-gray-400">Featured Image URL</label>
                    <input type="url" id="post-image" class="w-full mt-1 p-2 rounded-md form-input" placeholder="https://imgur.com/your-image.jpg" required>
                </div>
                <div>
                    <label for="post-summary" class="block text-sm font-medium text-gray-400">Summary (Short description for blog list page)</label>
                    <textarea id="post-summary" rows="3" class="w-full mt-1 p-2 rounded-md form-textarea" required></textarea>
                </div>
                <div>
                    <label for="post-content" class="block text-sm font-medium text-gray-400">Full Content (You can use basic HTML tags like &lt;b&gt;, &lt;p&gt;, &lt;ul&gt;)</label>
                    <textarea id="post-content" rows="10" class="w-full mt-1 p-2 rounded-md form-textarea" required></textarea>
                </div>
                <div class="flex justify-end space-x-4">
                    <button type="button" id="cancel-edit-btn" class="px-4 py-2 rounded-md btn-secondary hidden">Cancel Edit</button>
                    <button type="submit" id="submit-btn" class="px-6 py-2 rounded-md btn-primary">Publish Post</button>
                </div>
            </form>
        </div>
        
        <div class="bg-gray-800 p-6 rounded-lg">
            <h3 class="text-xl font-bold mb-4 gold-text">Published Posts</h3>
            <div id="posts-list" class="space-y-4"></div>
        </div>
    </main>
    
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
        import { getAuth, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
        import { getFirestore, collection, addDoc, onSnapshot, doc, getDoc, updateDoc, deleteDoc, serverTimestamp, query, orderBy } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";

        const firebaseConfig = { apiKey: "AIzaSyDbns1MdFW4nn8Sf0ROKnxidhIDSVYFUBc", authDomain: "codewithmee-7cb9d.firebaseapp.com", projectId: "codewithmee-7cb9d", storageBucket: "codewithmee-7cb9d.appspot.com", messagingSenderId: "19515490554", appId: "1:19515490554:web:57bb1609cad1d61524c943", measurementId: "G-3YJ8K05RPH" };
        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);
        const db = getFirestore(app);

        onAuthStateChanged(auth, (user) => { if (!user) window.location.href = './admin.html'; });

        const blogForm = document.getElementById('blog-form'), formTitle = document.getElementById('form-title'), submitBtn = document.getElementById('submit-btn'), cancelEditBtn = document.getElementById('cancel-edit-btn'), postsList = document.getElementById('posts-list');
        let editMode = false;

        blogForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            const postId = document.getElementById('post-id').value;
            const postData = {
                title: document.getElementById('post-title').value,
                summary: document.getElementById('post-summary').value,
                content: document.getElementById('post-content').value,
                imageUrl: document.getElementById('post-image').value,
                createdAt: serverTimestamp()
            };

            if (editMode && postId) {
                delete postData.createdAt;
                await updateDoc(doc(db, "blogPosts", postId), postData);
                alert('Post updated successfully!');
            } else {
                await addDoc(collection(db, "blogPosts"), postData);
                alert('Post published successfully!');
            }
            resetForm();
        });

        onSnapshot(query(collection(db, "blogPosts"), orderBy("createdAt", "desc")), (snapshot) => {
            postsList.innerHTML = '';
            if (snapshot.empty) { postsList.innerHTML = '<p class="text-gray-500">No posts found.</p>'; return; }
            snapshot.forEach((doc) => {
                const post = doc.data();
                const postEl = document.createElement('div');
                postEl.className = 'bg-gray-700 p-4 rounded-lg flex justify-between items-center';
                postEl.innerHTML = `
                    <div>
                        <h4 class="font-bold text-lg text-gray-100">${post.title}</h4>
                        <p class="text-xs text-gray-400">Published on: ${post.createdAt ? new Date(post.createdAt.seconds * 1000).toLocaleDateString() : 'N/A'}</p>
                    </div>
                    <div class="space-x-2">
                        <button class="px-3 py-1 text-sm rounded btn-secondary edit-btn" data-id="${doc.id}">Edit</button>
                        <button class="px-3 py-1 text-sm rounded btn-danger delete-btn" data-id="${doc.id}">Delete</button>
                    </div>
                `;
                postsList.appendChild(postEl);
            });
        });

        postsList.addEventListener('click', async (e) => {
            const target = e.target;
            const id = target.dataset.id;
            if (target.classList.contains('delete-btn')) {
                if (confirm('Are you sure you want to delete this post?')) {
                    await deleteDoc(doc(db, "blogPosts", id));
                    alert('Post deleted successfully!');
                }
            }
            if (target.classList.contains('edit-btn')) {
                const docRef = doc(db, "blogPosts", id);
                const docSnap = await getDoc(docRef);
                if (docSnap.exists()) { setupEditForm(docSnap.id, docSnap.data()); }
            }
        });

        function setupEditForm(id, data) {
            editMode = true;
            document.getElementById('post-id').value = id;
            document.getElementById('post-title').value = data.title;
            document.getElementById('post-summary').value = data.summary;
            document.getElementById('post-content').value = data.content;
            document.getElementById('post-image').value = data.imageUrl;
            formTitle.textContent = 'Edit Post';
            submitBtn.textContent = 'Update Post';
            cancelEditBtn.classList.remove('hidden');
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        function resetForm() {
            editMode = false; blogForm.reset(); document.getElementById('post-id').value = '';
            formTitle.textContent = 'Create New Post'; submitBtn.textContent = 'Publish Post';
            cancelEditBtn.classList.add('hidden');
        }
        cancelEditBtn.addEventListener('click', resetForm);
        
        document.getElementById('logout-btn').addEventListener('click', () => { signOut(auth).then(() => { window.location.href = './admin.html'; }); });
    </script>
</body>
</html>
